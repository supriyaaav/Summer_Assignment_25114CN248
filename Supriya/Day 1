#include<stdio.h>
int main()
{
    int n, sum=0;
    printf("enter a number :");
    scanf("%d",&n);
    sum =n*(n+1)/2;
    printf("the sum of first %d natural numbers is : %d",n,sum);
    
    return 0;
}
